// CurrentProjects.tsx
import React from "react";

const CurrentProjects: React.FC = () => {
  // Use hooks or component logic to fetch user-specific data
  return (
    <div className="content">{/* Render user-specific projects here */}</div>
  );
};

export default CurrentProjects;
